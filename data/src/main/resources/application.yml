server:
  port: 8080
# mybatis日志配置
mybatis:
  configuration:
    cache-enabled: true
spring:
  # 数据库配置
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/sky_take_out?useSSL=false&allowPublicKeyRetrieval=true
    type: com.alibaba.druid.pool.DruidDataSource
    password: 622717
